.container
  .row
    .tags-list
      %h2
        Tags list
      %ul
        - @tags.each do |tag|
          %li.col-md-4(style = "list-style: none")
            = link_to "#{tag.name}   #{tag.taggings_count} project", tag_path(tag.id),class:"btn btn-default", style: "list-style: none"
